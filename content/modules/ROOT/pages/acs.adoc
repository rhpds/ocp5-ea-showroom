= Advanced Cluster Security (ACS) 5

include::partial$_attributes.adoc[]

:page-toclevels: 2

== Feature Overview

Red Hat Advanced Cluster Security (ACS) 5 introduces several new capabilities for securing containerized and virtualized workloads:

. *VM Vulnerability Management* — extends RHACS vulnerability scanning to cover virtual machine workloads running on OpenShift Virtualization. The `roxagent` service runs inside VMs, scans installed packages (RPM/DNF), and reports vulnerabilities to ACS Central via Sensor over VSOCK with pull mode. Organizations gain unified vulnerability visibility across both containers and VMs from a single ACS console.

. *File Activity Monitoring (FAM)* — provides runtime file integrity monitoring. Security teams can define custom file paths, with wildcard support, to monitor for access, modification, and rename operations. File activity criteria can be combined with process-level criteria such as process name, ancestor, arguments, UID in deployment and node policies, enabling detection of unauthorized file access patterns required for compliance frameworks such as PCI-DSS.

. *OpenShift Lightspeed Integration for Risk Enrichment* — uses OpenShift Lightspeed to enrich ACS Risk findings with AI-assisted context: why a deployment ranks high, which signals drive the score, and what remediation to prioritize. Security teams can move from a ranked Risk list to an explained, actionable next step without leaving the console workflow.

. *ACS MCP Server* — exposes ACS Vulnerability Management APIs (CVEs, images, deployments, and nodes) and Configuration Management APIs (secured clusters) as MCP tools, allowing AI agents to query security data programmatically. 

. *Init Container Security Coverage* — RHACS now evaluates init containers for both image scanning and security policies. When a deployment references init containers, their images are scanned for vulnerabilities using the same process as main container images. Build and Deploy stage policies can be configured to include or exclude init containers using the "Filters" menu.

Each of these features is covered in more detail in the following sections. Part of each walkthrough will be to discuss how the feature is setup, how it works, and how it can be used to secure your environment.

====
*The ACS team asks that you provide feedback on the feature by completing the structured feedback form at the end of each exercise.*
====

== Before You Begin

* Register for the link:https://docs.google.com/forms/d/e/1FAIpQLSdjrEjL3lCfmTTVEwwMct-H72UNZ729RsIkUQ8Wvp5yHmtrOw/viewform[ACS 5.0 Feedback Form^]. You can track your feedback in the doc and there is a link to the form at the end of each exercise.

* Next, confirm you can open the OpenShift console and ACS Central tabs in the Showroom UI. Log in with these credentials:
+
[cols="1,1,1,1"]
|===
| *Service* | *Username* | *Password* | *Link*

| OpenShift Console
| `{openshift_cluster_admin_username}`
| `{openshift_cluster_admin_password}`
| {openshift_cluster_console_url}[Console^]

| ACS Central
| `admin`
| `{common_password}`
| https://central-acs.{openshift_cluster_ingress_domain}[ACS Central^]
|===

* Verify the Secured Cluster is connected: navigate to *Platform Configuration > Clusters* in ACS Central and confirm the Secured Cluster is listed.

* Verify the prebuilt VM is running:
+
[source,sh,subs="attributes",role=execute]
----
oc get vmi -n acs-virt
----
+
You should see `rhel9-vm` in a `Running` phase.

* Deploy the RHACS / OpenShift Security Roadshow demo applications. 
+
IMPORTANT: These are intentionally vulnerable workloads and are not recommended for production environments.
+
[source,sh,subs="attributes",role=execute]
----
cd ~/ && git clone https://github.com/mfosterrox/demo-applications.git
cd ~/demo-applications
oc apply -f k8s-deployment-manifests/-namespaces/
oc apply -R -f k8s-deployment-manifests/juice-shop/
oc apply -R -f k8s-deployment-manifests/log4shell/
oc apply -R -f k8s-deployment-manifests/webgoat/
oc apply -R -f k8s-deployment-manifests/dvwa/
oc apply -R -f k8s-deployment-manifests/acs-fam-demo/
----
+
Confirm the File Activity Monitoring (FAM) trigger and a sample vulnerable app are running:
+
[source,sh,subs="attributes",role=execute]
----
oc get deploy,pods -n acs-fam-demo
oc get deploy,pods -n juice-shop
----
+
You should see `fam-target` & `juice-shop` in `Running` phase. 

* Verify the ACS MCP server is available:
+
[source,sh,subs="attributes",role=execute]
----
oc get pods -n acs-mcp
oc get route -n acs-mcp
----
+
Output should look like this:
+
[source,bash]
----
oc get route -n acs-mcp
NAME                                          READY   STATUS      RESTARTS   AGE
acs-mcp-server-stackrox-mcp-b79597899-lz64s   1/1     Running     0          3h5m
copy-central-ca-8jrsn                         0/1     Completed   0          178m
NAME                          HOST/PORT                                                                    PATH   SERVICES                      PORT    TERMINATION          WILDCARD
acs-mcp-server-stackrox-mcp   acs-mcp-server-stackrox-mcp-acs-mcp.apps.ocp.2bfz7.sandbox5240.opentlc.com          acs-mcp-server-stackrox-mcp   https   reencrypt/Redirect   None
----

Awesome! You're ready to start the exercises.

== Hands-on Exercises

=== Exercise 1: VM Vulnerability Management

ACS 5 extends vulnerability scanning to virtual machines running on OpenShift Virtualization. A `roxagent` service inside the guest reports installed packages to Sensor over VSOCK, so you get unified CVE visibility across containers and VMs from a single ACS console.

In this exercise you will verify a prebuilt RHEL VM, review its findings in ACS, and optionally onboard a second VM.

[.step-title]
Understanding the prerequisites for VM vulnerability scanning

Before you verify the prebuilt VM, review what RHACS requires to scan OpenShift Virtualization workloads. These conditions are already met in this lab for `rhel9-vm`.

For full product details, see the link:https://docs.redhat.com/en/documentation/red_hat_advanced_cluster_security_for_kubernetes/4.11/html-single/operating/index#scanning-virtual-machines_examine-images-for-vulnerabilities[Scanning virtual machines^] topic in the RHACS 4.11 Operating guide.

[.step-title]
How RHACS scans virtual machines

RHACS collects, processes, and analyzes VM package data with these components:

`roxagent`::
Quadlet (Podman container managed by systemd) that runs inside the VM. It scans installed packages, caches index reports, and runs in server mode to answer authenticated Sensor requests over VSOCK. In ACS 5.0, this is the supported customer-shipped form of `roxagent`; standalone binaries are used only for local demos and are not shipped to customers.

Sensor::
Cluster-level component that pulls index reports from `roxagent` over VSOCK (via the KubeVirt API), aggregates VM data with Kubernetes API information, and sends it to Central.

Central::
Stores VM information, matches packages against known CVEs (via Scanner V4), and exposes results in the RHACS portal and API.

[.step-title]
Prerequisites

OpenShift Virtualization and RHACS must meet the following requirements in your environment:

NOTE: These requirements are met in this lab.

* The OpenShift Virtualization Operator is installed on the cluster you want to scan.
* The `HyperConverged` resource is patched to enable virtual socket (vsock) support.
* The RHACS feature flag `ROX_VIRTUAL_MACHINES=true` is set on the Central container and the Sensor container.
* Sensor has RBAC permission to use VSOCK (`GET` on the `virtualmachineinstances/vsock` resource).

Each VM to be scanned must meet the following requirements:

* The VM runs Red Hat Enterprise Linux (RHEL) and is registered with Red Hat Subscription Manager with an active RHEL subscription. VM vulnerability scanning does not cover non-RHEL guest operating systems in this release.
* VSOCK is enabled on the VM with `spec.domain.devices.autoattachVSOCK: true`.
* The VM has internet access to download repository-to-Common Platform Enumeration (CPE) mappings.
* DNF is installed.
* `roxagent` runs with privileges sufficient to scan package databases and expose index reports over VSOCK (typically root or sudo).
* At least one DNF transaction (for example, `dnf install`) has been executed on the system to prepopulate required metadata.

[.step-title]
Verifying and reviewing the prebuilt VM in ACS

.Procedure
. Confirm the prebuilt VM instance is running:
+
[source,sh,subs="attributes",role=execute]
----
oc get vmi -n acs-virt
----
+
Output:
[source,bash]
----
oc get vmi -n acs-virt
NAME       AGE     PHASE     IP            NODENAME
rhel9-vm   10m     Running   10.129.2.10   ip-10-0-161-177.ec2.internal
----

You should see `rhel9-vm` in a `Running` phase. You can also confirm in the OpenShift console under *Virtualization > VirtualMachines*.

[start=2]
. In the OpenShift console, go to *Virtualization > VirtualMachines* and select the `acs-virt` project.
+
Confirm that `rhel9-vm` is listed and shows a *Running* status.
+
image::acs-vm-openshift-virtualmachines.png[OpenShift console VirtualMachines page showing rhel9-vm Running in the acs-virt project,link=_images/acs-vm-openshift-virtualmachines.png,window=ea-images]

. Open the web console for `rhel9-vm` and log in:
+
On the `rhel9-vm` overview, use *Open web console* (or open the *Console* tab).
+
Log in with the following credentials:
+
Username:
+
[source,text,role="copypaste",subs="+attributes"]
----
{vm_user}
----
+
Password:
+
[source,text,role="copypaste",subs="+attributes"]
----
{vm_password}
----
+
Run the remaining guest commands in this procedure from the VM console, not from the Showroom terminal.

. Verify that `roxagent` is installed and healthy inside the VM. Run these from the VM console:
+
[source,sh,role="copypaste"]
----
command -v roxagent
sudo journalctl -u roxagent.service --no-pager -n 20
----
+
`command -v` should print the `roxagent` path. The journal should show the `roxagent` unit running `serve --port 818` (VSOCK pull) and a completed scan, for example `Initial scan complete, report cached. Num packages:`.

. In ACS Central, open *Vulnerability Management > Results > Virtual Machines*.
+
You should see the `rhel9-vm` workload listed for the `acs-virt` namespace, including CVE counts by severity.
+
image::acs-vm-vulnerabilities-list.png[ACS Virtual machine vulnerabilities page listing rhel9-vm with CVE severity counts,link=_images/acs-vm-vulnerabilities-list.png,window=ea-images]

. Click `rhel9-vm` and review the reported CVEs and related details.
+
Use the *Vulnerabilities*, *Components*, and *Details* tabs to explore findings for the guest OS.
+
image::acs-vm-rhel9-cve-details.png[ACS vulnerability details for rhel9-vm showing CVE severity summary and CVE table,link=_images/acs-vm-rhel9-cve-details.png,window=ea-images]

* Do you like what you see? 
* What would you like to see improved? 
* What workflows would you like to see supported?

[.step-title]
Create and onboard a second VM (Optional)

In this optional path you deploy a second VirtualMachine from a declarative GitOps-style manifest, then confirm ACS scans it alongside `rhel9-vm`.

IMPORTANT: Schedule the VM on a *worker* node.

. Speed up Sensor VM scraping for this demo. Set the poll interval to `1m` so a new VM appears in ACS shortly after `roxagent` is listening (the default can take about an hour). This only controls how often Sensor asks listening VMs for their current report; it does not change guest-side package rescans.
+
[source,sh,subs="attributes",role=execute]
----
oc set env deploy/sensor -n {acs_namespace} ROX_VIRTUAL_MACHINES_SCRAPER_POLL_INTERVAL=1m
----

. Create the cloud-init Secret for the VM. This userdata:
+
* Stores cloud-init config in a Secret
* Registers the guest and enables RHEL 9.6 repos so ACS can map packages to CVEs
* Installs extra packages (`java-17-openjdk`, `python3`, `php`, `postgresql`, `nodejs`, `git`) so this VM reports a different CVE set than `rhel9-vm`
* Installs `roxagent` and starts it in server mode so Sensor can pull package inventory over VSOCK
+
[source,yaml,role=execute]
----
oc apply -f - <<'EOF'
include::example$rhel-webserver-cloudinit-secret.yaml[]
EOF
----

. Create the VirtualMachine. This manifest:
+
* Enables VSOCK so ACS Sensor can pull index reports from `roxagent`
* References the cloud-init Secret from the previous step
* Boots the same primed RHEL 9 containerDisk as `rhel9-vm` (`quay.io/rhpds/ocp5-vm-primed:rhel9`) so the guest starts without cloning a 30Gi DataSource
+
[source,yaml,role=execute]
----
oc apply -f - <<'EOF'
include::example$rhel-webserver-vm.yaml[]
EOF
----

. Wait until the VirtualMachineInstance is running. Cloud-init still registers the guest, updates packages, and installs `roxagent` after the VMI is `Running`:
+
[source,sh,subs="attributes",role=execute]
----
oc wait --for=jsonpath='{.status.phase}'=Running vmi/rhel-webserver -n acs-virt --timeout=300s
----
+
Confirm the VMI status and guest IP:
+
[source,sh,subs="attributes",role=execute]
----
oc get vmi rhel-webserver -n acs-virt
----
+
TIP: The VMI should reach `Running` within a couple of minutes. First-boot cloud-init (subscription, packages, `roxagent`) can still take several minutes after that.

. Optionally verify `roxagent` inside the guest. Open the console (*Virtualization > VirtualMachines > rhel-webserver > Console*) and log in:
+
Username:
+
[source,text,role="copypaste"]
----
cloud-user
----
+
Password:
+
[source,text,role="copypaste"]
----
redhat
----
+
[source,bash,role="copypaste"]
----
command -v roxagent
sudo grep 'index report containing' /var/log/roxagent.log
----
+
A line such as `Sent message with index report containing 492 packages to host` means the agent already pushed inventory. The process does not have to still be running.

. Return to ACS Central → *Vulnerability Management > Results > Virtual Machines* and confirm `rhel-webserver` appears alongside `rhel9-vm`.
+
With `ROX_VIRTUAL_MACHINES_SCRAPER_POLL_INTERVAL=1m`, allow about one to two minutes after `roxagent` is listening for Sensor to pull the first report and for results to show. Without that setting, first results can take up to an hour.

[.step-title]
Structured feedback for VM Vulnerability Management

Please help us improve by submitting feedback for VM Vulnerability Management:

link:https://docs.google.com/forms/d/e/1FAIpQLSdjrEjL3lCfmTTVEwwMct-H72UNZ729RsIkUQ8Wvp5yHmtrOw/formResponse[ACS 5.0 Feedback Form — VM Vulnerability Management^]

=== Exercise 2: File Activity Monitoring (FAM)

File Activity Monitoring watches file access, modification, and rename operations on paths you define, including wildcards, and can combine those events with process context. That gives runtime visibility that image scanning alone cannot provide, and it supports compliance controls such as PCI-DSS.

In this exercise you will confirm FAM is enabled, review the lab policies, and trigger a deployment-scoped violation.

[.step-title]
Understanding the prerequisites for File Activity Monitoring (FAM)

Before you trigger a violation, review what RHACS requires for file activity monitoring. These conditions are already met in this lab.

For full product details, see the link:https://docs.redhat.com/en/documentation/red_hat_advanced_cluster_security_for_kubernetes/4.11/html-single/operating/index#configuring-file-activity-monitoring_using-policies-to-provide-security[Configuring file activity monitoring^] topic in the RHACS 4.11 Operating guide.

.How RHACS monitors file activity

Collector (per node)::
Observes file operations on monitored paths for containerized workloads and for host processes, then forwards events to Sensor.

Sensor::
Evaluates runtime policies against those events and reports matches to Central.

Central::
Stores violations and exposes them in the portal (deployment events) and API (including node events).

.Prerequisites

RHACS and the secured cluster must meet the following requirements:

* File Activity Monitoring is enabled on the SecuredCluster under `spec.perNode.fileActivityMonitoring.mode: Enabled`.
* Runtime policies use the *File Path* criterion (RHACS 4.11+ field) with either a *Deployment* or *Node* event source.
* The architecture is x86 (currently the only architecture supported for FAM).

NOTE: Node-event violations appear on the Violations page in the RHACS portal, and are also available through notifiers and the `/v1/alerts` API. Machine Config Operator file changes are not detected.

[.step-title]
Review the preconfigured FAM policies

First, confirm FAM is enabled on the SecuredCluster:

[source,sh,subs="attributes",role=execute]
----
oc get securedcluster -n acs -o jsonpath='{.items[0].metadata.name}{"\n"}{.items[0].spec.perNode.fileActivityMonitoring.mode}{"\n"}'
----

You should see the SecuredCluster name and `Enabled`.

With FAM enabled, this environment contains two SecurityPolicy resources deployed via GitOps:

* *Deployment File Activity* — `eventSource: DEPLOYMENT_EVENT`, monitors sensitive paths such as `/etc/{asterisk}{asterisk}/{asterisk}`, `/boot/{asterisk}{asterisk}/{asterisk}`, and `/usr/{asterisk}{asterisk}/{asterisk}` for container/workload file activity.
* *Node File Activity* — `eventSource: NODE_EVENT`, monitors the same path patterns for host-level processes (SSH, systemd, and similar).

Review the policies in ACS Central:

. In ACS Central, open *Platform Configuration > Policy Management*.
+
Search for `File Activity` and open *Deployment File Activity*.
+
Confirm:

* *Lifecycle* is *Runtime*
* *Event source* is *Deployment*
* Policy criteria include *File Path* values such as `/etc/{asterisk}{asterisk}/{asterisk}`

. Open *Node File Activity* and confirm the event source is *Node* with the same style of *File Path* criteria.
+
TIP: The GitOps source for these policies is in the lab GitOps repository under `acs/acs-instance/templates/fim-deployment-policy.yaml` and `fim-node-policy.yaml`.

. Confirm the FAM trigger workload from *Before You Begin* is running:
+
[source,sh,subs="attributes",role=execute]
----
oc get deploy,pods -n acs-fam-demo
----
+
You should see `fam-target` with a pod in `Running` phase.

[.step-title]
Trigger and review a deployment FAM violation

. Allow a root debug pod in `acs-fam-demo`. New namespaces default to Pod Security `restricted:latest`, which rejects `oc debug --as-root` (`runAsUser=0`). Label the namespace privileged and grant the `privileged` SCC to the default service account:
+
[source,sh,subs="attributes",role=execute]
----
oc label namespace acs-fam-demo \
  pod-security.kubernetes.io/enforce=privileged \
  pod-security.kubernetes.io/audit=privileged \
  pod-security.kubernetes.io/warn=privileged \
  security.openshift.io/scc.podSecurityLabelSync=false --overwrite
oc adm policy add-scc-to-user privileged -z default -n acs-fam-demo
----

. Trigger a monitored file operation inside the FAM target. `oc debug --as-root` still records as a *deployment* file-activity event for FAM:
+
[source,sh,subs="attributes",role=execute]
----
oc debug -n acs-fam-demo deploy/fam-target --as-root -- touch /etc/passwd
----
+
This matches the *Deployment File Activity* policy (`/etc/{asterisk}{asterisk}/{asterisk}` under a deployment event source).

. In ACS Central, open *Violations*.
+
You should see the violation at the top of the page. Otherwise, filter by policy name `Deployment File Activity`.
+
Open a matching violation and review the runtime detail (file path, process context, deployment, and timestamp).

. Optionally confirm the alert count via the ACS API (basic auth):
+
[source,bash,role="execute",subs="+attributes"]
----
CENTRAL="https://central-acs.{openshift_cluster_ingress_domain}"
curl -sk -u "admin:{common_password}" -G "${CENTRAL}/v1/alerts/summary/groups" \
  --data-urlencode 'query=Policy:"Deployment File Activity"' | jq .
----
+
You should see `numAlerts` greater than zero for that policy.

What you just proved:: FAM on the secured cluster observed a container file operation on a monitored path, Sensor evaluated the *Deployment File Activity* runtime policy, and Central recorded a Violations-page alert.

[.step-title]
Trigger a node FAM violation

Use this path if you want to exercise host-level file activity. Node FAM alerts appear on the Violations page in the RHACS portal, and are also available through the API and notifiers.

. Trigger a host file operation via `nsenter` into PID 1 on a worker node:
+
[source,sh,subs="attributes",role=execute]
----
NODE=$(oc get nodes -l node-role.kubernetes.io/worker -o jsonpath='{.items[0].metadata.name}')
echo "Using node: ${NODE}"
oc debug "node/${NODE}" --quiet -- bash -c 'nsenter -t 1 -m -u -i -n -p touch /etc/passwd'
----
+
IMPORTANT: Plain `oc debug node` shells are classified as *deployment* events. The `nsenter` into PID 1 is required so RHACS records a *NODE_EVENT*.

. In ACS Central, open *Violations*. Select the *Nodes* tab, then you should see a recent node violation. The Deployments view does not list this policy. You can also query the alert count via the API:
+
[source,bash,role="execute",subs="+attributes"]
----
CENTRAL="https://central-acs.{openshift_cluster_ingress_domain}"
curl -sk -u "admin:{common_password}" -G "${CENTRAL}/v1/alerts/summary/groups" \
  --data-urlencode 'query=Policy:"Node File Activity"' | jq .
----
+
NOTE: Node FAM alerts now appear on the *Violations* page, and remain available through the API and notifiers.

[.step-title]
Structured feedback for File Activity Monitoring

Please help us improve by submitting feedback for File Activity Monitoring:

link:https://docs.google.com/forms/d/e/1FAIpQLSdjrEjL3lCfmTTVEwwMct-H72UNZ729RsIkUQ8Wvp5yHmtrOw/formResponse[ACS 5.0 Feedback Form — File Activity Monitoring^]

=== Exercise 3: OpenShift Lightspeed Integration for Risk Enrichment

OpenShift Lightspeed can enrich ACS Risk findings with AI-assisted context: why a deployment ranks high, which signals drive the score, and what to remediate first. Security teams can move from a ranked list to an explained next step without leaving the console.

In this exercise you will open ACS Risk for the demo workloads and use Lightspeed to explain and prioritize findings.

[.step-title]
Understanding Risk Enrichment with Lightspeed (TBD)

.Prerequisites

[.step-title]
Explore ACS Risk and enrich with Lightspeed (TBD)

[.step-title]
Structured feedback (OpenShift Lightspeed Integration for Risk Enrichment)

Thank you for completing this exercise! Please help us improve by submitting feedback for OpenShift Lightspeed Integration for Risk Enrichment:

link:https://docs.google.com/forms/d/e/1FAIpQLSdjrEjL3lCfmTTVEwwMct-H72UNZ729RsIkUQ8Wvp5yHmtrOw/formResponse[ACS 5.0 Feedback Form — OpenShift Lightspeed Integration for Risk Enrichment^]

=== Exercise 4: ACS MCP Server

The ACS MCP server exposes Vulnerability Management and Configuration Management APIs as MCP tools. AI agents, including OpenShift Lightspeed, can query CVEs, images, deployments, nodes, and secured clusters in natural language instead of clicking through Central page by page.

In this exercise you will verify the preinstalled MCP endpoint and query ACS data through Lightspeed.

[.step-title]
Understanding the ACS MCP server

The ACS MCP server is based on https://github.com/stackrox/stackrox-mcp[stackrox-mcp^] (same approach as the RHACS demo `mcp-server-setup`). It exposes RHACS Central data to MCP-compatible clients through toolsets such as:

* *Vulnerability management* — query CVEs, images, deployments, and nodes (including `get_deployments_for_cve` and `get_nodes_for_cve`)
* *Configuration management* — list secured clusters (read-only in this lab). There is no MCP tool for policies; review FAM and other policies in ACS Central as you did in Exercise 2.

.How it fits together

ACS MCP server::
Runs in the `acs-mcp` namespace, speaks streamable HTTP MCP on `/mcp`, and calls Central on behalf of the client.

Authentication::
External clients use *passthrough* auth: your MCP client sends `Authorization: Bearer <ACS API token>`. OpenShift Lightspeed is pre-wired with an Analyst token via secret `acs-mcp-api-token`.

AI client::
OpenShift Lightspeed (in-console) or an external MCP client (Claude Code, Cursor, Claude Desktop) discovers tools and answers natural-language security questions.

.Prerequisites

* The ACS MCP server Deployment and Route are available in `acs-mcp`.
* Central is reachable and the secured cluster is reporting data.
* For Lightspeed: OLSConfig includes the `MCPServer` feature gate and an `acs-mcp` server entry.
* For an external client: an ACS API token with at least the *Analyst* role.

[.step-title]
Verify the preinstalled ACS MCP server

. Confirm the MCP pod and Route:
+
[source,sh,subs="attributes",role=execute]
----
oc get pods -n acs-mcp
oc get route -n acs-mcp
----

. Check the health endpoint:
+
[source,sh,subs="attributes",role=execute]
----
MCP_HOST=$(oc get route -n acs-mcp -o jsonpath='{.items[0].spec.host}')
echo "MCP endpoint: https://${MCP_HOST}/mcp"
curl -sk "https://${MCP_HOST}/health"
----
+
You should see `{"status":"ok"}`.
+
TIP: Keep the printed MCP endpoint URL — you will need it if you connect an external client.

. Confirm Lightspeed is wired to ACS MCP:
+
[source,sh,subs="attributes",role=execute]
----
oc get olsconfig cluster -n openshift-lightspeed -o jsonpath='{range .spec.mcpServers[*]}{.name}{" -> "}{.url}{"\n"}{end}'
oc get secret acs-mcp-api-token -n openshift-lightspeed
----
+
You should see an `acs-mcp` entry pointing at the in-cluster ACS MCP service URL, and the API token secret present.

[.step-title]
Query ACS through OpenShift Lightspeed

. Open the OpenShift console and launch *OpenShift Lightspeed* (chat icon).
+
If prompted to approve MCP tools, approve the ACS MCP tools so the agent can call them.

. Ask Lightspeed to list secured clusters (warm-up, from the stackrox-mcp / RHACS demo examples):
+
----
List all clusters secured by ACS / StackRox.
----

. Ask a vulnerability question that exercises the vulnerability toolset:
+
----
Is CVE-2021-44228 (Log4Shell) detected in any of my clusters or deployments?
----
+
TIP: This lab deploys intentionally vulnerable demo apps (including log4shell). Compare the agent answer with *Vulnerability Management* in ACS Central.

. Ask a multi-CVE question that should resolve deployments and namespaces for each CVE:
+
----
Are any of the following CVEs present in my clusters: CVE-2025-47151, CVE-2024-47561? Show which deployments and namespaces are affected by each.
----
+
TIP: This prompt tests whether the agent can handle more than one CVE in a single request.

. Ask whether cluster nodes are affected (this should invoke `get_nodes_for_cve`):
+
----
Are any of my cluster nodes affected by CVE-2021-44228?
----

. Optionally try more targeted prompts. ACS MCP looks up *named* CVEs (and can filter by namespace). It does not enumerate every critical CVE in a namespace:
+
----
Which deployments in the juice-shop or log4shell namespaces are affected by CVE-2021-44228?
----
+
----
Use the ACS MCP server and run list_secured_clusters.
----

What you just proved:: The ACS MCP server translated natural-language questions into RHACS API tool calls (secured clusters, CVE lookups for deployments and nodes), and Lightspeed returned security data without you running `roxctl` or clicking through every Central page manually.

[.step-title]
Connect an external MCP client (Optional)

Use this path if you want to connect Claude Code, Cursor, or Claude Desktop the way the RHACS demo documents.

. Generate an Analyst API token in Central:
+
[source,bash,role="execute",subs="+attributes"]
----
CENTRAL="https://central-acs.{openshift_cluster_ingress_domain}"
curl -sk -u "admin:{common_password}" \
  -X POST "${CENTRAL}/v1/apitokens/generate" \
  -H "Content-Type: application/json" \
  -d '{"name":"acs-mcp-lab","roles":["Analyst"]}' | jq -r .token
----
+
Copy the token value. You can also create a token in ACS Central under *Platform Configuration > Integrations > API Token*.

. Note the public MCP endpoint:
+
[source,sh,subs="attributes",role=execute]
----
echo "https://$(oc get route -n acs-mcp -o jsonpath='{.items[0].spec.host}')/mcp"
----

. Configure *Claude Code*:
+
[source,bash,role="copypaste"]
----
claude mcp add --transport http acs \
  https://<acs-mcp-route-host>/mcp \
  --header "Authorization: Bearer <paste-your-token-here>"
----

. Or add this to Claude Desktop / Cursor MCP settings:
+
[source,json,role="copypaste"]
----
{
  "mcpServers": {
    "acs": {
      "url": "https://<acs-mcp-route-host>/mcp",
      "headers": {
        "Authorization": "Bearer <paste-your-token-here>"
      }
    }
  }
}
----
+
Replace `<acs-mcp-route-host>` and `<paste-your-token-here>` with the values from the previous steps.

. In your external client, run the same prompts as in the Lightspeed section (list clusters, Log4Shell CVE, multi-CVE deployments, node CVE) and confirm tools are invoked successfully.

[.step-title]
Structured feedback on the ACS MCP Server

Thank you for completing this exercise! Please help us improve by submitting feedback for ACS MCP Server:

link:https://docs.google.com/forms/d/e/1FAIpQLSdjrEjL3lCfmTTVEwwMct-H72UNZ729RsIkUQ8Wvp5yHmtrOw/formResponse[ACS 5.0 Feedback Form — ACS MCP Server^]

=== Exercise 5: Init Container Security Coverage

Init containers run setup work and then exit before the application starts. Because they use the same cluster credentials and image pull path as the rest of the Pod, a vulnerable or over-privileged init image can compromise the workload before the main container ever runs. ACS 5 scans init container images and evaluates Build and Deploy policies against them, with the same workflow you already use for application containers.

In this exercise you will deploy four workloads that isolate those capabilities, then review the violations ACS attributes to the init containers.

The four use cases are:

* *Clean* -- an init container with nothing out of the ordinary
* *Dirty* -- an init container based on an image with known CVEs
* *Blue* -- an init container that performs a privileged action with justification
* *Red* -- an init container that uses an unpatched image with known CVEs and performs a privileged action with no justification

[.step-title]
Prepare the test project and policies

.Procedure
. Create a project and grant the default service account the `privileged` SCC (required for Blue and Red):
+
[source,sh,subs="attributes",role=execute]
----
oc new-project acs-init-container-test
oc adm policy add-scc-to-user privileged -z default -n acs-init-container-test
----

. Confirm the project has no workloads:
+
[source,sh,subs="attributes",role=execute]
----
oc get pods -n acs-init-container-test
----

. Apply two additional policies scoped to this namespace only:
+
[source,yaml,role=execute]
----
oc apply -f - <<'EOF'
include::example$fixable-important-cve.yaml[]
EOF
----
+
[source,yaml,role=execute]
----
oc apply -f - <<'EOF'
include::example$privileged-init-container.yaml[]
EOF
----

. Review the two policy definitions. For the privileged policy, note:
+
[source,text]
----
policyName: "EAP-Init-Test: Privileged Container (Blue/Red)"
description: "Demo policy for the RHACS 5.0 EAP init-container test workflow. Fires on any privileged container - main or init - inside the acs-init-container-test namespace. Used to validate that Build/Deploy policy evaluation reaches init containers (Blue = legitimate/noise, Red = illegitimate/true-positive)."
rationale: "Containers running as privileged represent greater post-exploitation risk by allowing an attacker to access all host devices, run a daemon in the container, etc."
remediation: "Verify that privileged capabilities are actually required (e.g. sysctl tuning) and cannot be provided via a narrower capability grant."
----
+
For the CVE policy, note:
+
[source,text]
----
policyName: "EAP-Init-Test: Fixable Important+ CVE (Dirty/Red)"
description: "Demo policy for the RHACS 5.0 EAP init-container test workflow. Fires when any container image - main or init - in the acs-init-container-test namespace has a fixable CVE with CVSS >= 7. Used to validate that image vulnerability scanning/policy evaluation attributes findings to init containers (Dirty and Red should match; Clean and Blue should not)."
rationale: "Fixable vulnerabilities above this severity threshold represent real, addressable risk and should not be shipped in any container, including init containers."
remediation: "Rebuild the image against an updated base to pick up the available fix."
----

. In ACS Central, open the *Dashboard*. Set *Resources* to `local-cluster` and the namespace filter to `acs-init-container-test`.
+
image::acs-clusters.png[ACS Dashboard resource filter set to local-cluster,link=_images/acs-clusters.png,window=ea-images]
+
There should be no violations yet. You have policies, but no workloads.

NOTE: The ACS Dashboard is timestamped and does not auto-refresh. Reload the page after each deployment before you judge the result.

[.step-title]
Run the Clean scenario

.Procedure
. Deploy the Clean workload:
+
[source,yaml,role=execute]
----
oc apply -f - <<'EOF'
include::example$clean-deployment.yaml[]
EOF
----
+
This creates `clean-app` with a `clean-init` init container that performs no dangerous actions.

. In the OpenShift console, go to *Workloads > Deployments*, open `clean-app` in `acs-init-container-test`, and select the *YAML* tab.
+
Confirm the `clean-init` definition. ACS has detected and scanned this init container and should not raise the two demo policies.
+
image::acs-deployment-clean.png[Clean deployment YAML showing the clean-init container,link=_images/acs-deployment-clean.png,window=ea-images]

. Return to the ACS Dashboard (still filtered to `local-cluster` / `acs-init-container-test`) and reload the page.
+
You should see at most a low policy violation. *Active images at most risk* lists two images. One of them is the init image, `distroless/base-debian12`.

[.step-title]
Run the Dirty scenario

.Procedure
. Deploy the Dirty workload:
+
[source,yaml,role=execute]
----
oc apply -f - <<'EOF'
include::example$dirty-deployment.yaml[]
EOF
----

. In the OpenShift console, go to *Workloads > Topology*, select the `dirty-app` workload, then open the running Pod. On the *Logs* tab, use the container pulldown and select `dirty-init`.
+
NOTE: A Pod can have more than one container. The pulldown includes init containers, so you can read `dirty-init` even after it has completed.
+
The log shows a well-behaved init container. The risk is the image, not the command it ran.

. Reload the ACS Dashboard with the same cluster and namespace filters.
+
ACS has scanned the Dirty init container during deploy and should now show *High* violations. Click the *High* panel and confirm the findings are attributed to the init container, not only the pause main container.
+
image::acs-deployment-dirty.png[ACS violation details for the Dirty init container,link=_images/acs-deployment-dirty.png,window=ea-images]
+
NOTE: RHACS detected and reported image violations on the init container of the Dirty deployment.

[.step-title]
Run the Blue scenario

.Procedure
. Deploy the Blue workload:
+
[source,yaml,role=execute]
----
oc apply -f - <<'EOF'
include::example$blue-deployment.yaml[]
EOF
----
+
This init container switches to privileged and performs a legitimate action (`sysctl` for `vm.max_map_count`).

. In *Workloads > Topology*, confirm `blue-app` is running (blue border on the workload). Open the Pod *Logs* tab and select `blue-init`.

. Reload the ACS Dashboard (`local-cluster`, namespace `acs-init-container-test`).
+
You should see additional policy violations for the privileged init container, and a change in *Risk Priority* under *Active images at most risk*.
+
NOTE: Blue is a real privileged-policy hit with an operational justification. Treat it as the noise case when you compare it to Red.

[.step-title]
Run the Red scenario

.Procedure
. Deploy the Red workload:
+
[source,yaml,role=execute]
----
oc apply -f - <<'EOF'
include::example$red-deployment.yaml[]
EOF
----

. In *Workloads > Topology*, open `red-app`, then the running Pod, then *Logs* for `red-init`. The log should read:
+
----
red-init: unpatched image with known CVEs, running privileged with no legitimate justification
----

. Reload the ACS Dashboard (`local-cluster`, namespace `acs-init-container-test`). You should now see critical CVE and privileged violations for the Red init container:
+
image::acs-deployment-red.png[ACS Dashboard showing critical CVE and privileged violations for the Red init container,link=_images/acs-deployment-red.png,window=ea-images]
+
NOTE: The distroless images in this namespace are the init-container images. These four deployments show that the ACS policy engine evaluates init containers as well as the main container on the deployment.

What you just proved:: ACS 5 scanned the init container images, attributed findings to those init containers, and fired namespace-scoped Build/Deploy policies on Dirty (CVEs), Blue (justified privileged), and Red (CVEs plus unjustified privileged). Clean stayed quiet on both demo policies.

[.step-title]
Structured feedback (Init Container Security Coverage)

Thank you for completing this exercise! Please help us improve by submitting feedback for Init Container Security Coverage:

link:https://docs.google.com/forms/d/e/1FAIpQLSdjrEjL3lCfmTTVEwwMct-H72UNZ729RsIkUQ8Wvp5yHmtrOw/formResponse[ACS 5.0 Feedback Form — Init Container Security Coverage^]

== Key Takeaways

* ACS 5 extends security coverage beyond containers to virtual machines running on OpenShift Virtualization
* roxagent listens over VSOCK for Sensor pull requests, requiring no guest network configuration for ACS scanning
* File activity monitoring provides visibility into security-relevant file access patterns
* Lightspeed Risk Enrichment (when available) explains and prioritizes ACS Risk findings with AI-assisted context
* The ACS MCP server enables AI agents to query secured clusters and CVE findings (deployments and nodes) programmatically
* Init container scanning extends image and policy coverage to init containers
